# SkillSync Backend (FastAPI)
See conversation for full details. To run quickly for development:
- install requirements
- set DATABASE_URL in .env (you can use sqlite also)
- run `uvicorn app.main:app --reload`
